import { URIProtocolState, createURIProtocolState } from "../state";
import { BaseEvent } from "aerial-common2"

export const uriProtocolReducer = (state: URIProtocolState = createURIProtocolState(), event: BaseEvent) => {
  return state;
}